| Timestamp | Operation | Summary | Pages affected |
|---|---|---|---|
| 2026-09-01T00:00:00Z | /wiki ingest | Initial wiki setup from the hackathon landing page PDF (lablab.ai, saved 2026-09-01). Captured challenge requirements (autonomous agent + MCP/CLI + options trading), account rules (fresh paper account, $100k starting balance), prize pool ($6,300) and judging criteria, submission checklist, event schedule (28 Aug–4 Sep 2026), Alpaca/Featherless AI tech stack, and a snapshot of ~40 competing teams' pitches and tech stacks for competitive scan. | README.md, challenge-and-requirements.md, prizes-and-judging.md, submission-and-guidelines.md, tech-stack-and-partners.md, competing-projects.md |
| 2026-09-01T00:00:00Z | /research | Antigravity CLI research on options strategies for LLM agents (VRP harvesting, GEX positioning, earnings IV crush, catalyst momentum), zero-trust risk gate pipeline (schema validation → Kelly sizing → drawdown/spread gate → kill switch), underused signal sources (SEC EDGAR real-time, GEX, FRED net liquidity, IV/RV spread, earnings implied-move mismatch, vol term structure), and production architecture pattern ("LLM-as-Analyst, Quant-as-Governor, Code-as-Executor") with judging-relevant evaluation metrics (Sharpe/Sortino, max drawdown, profit factor, slippage, audit trace). | trading-strategy-and-architecture-research.md, README.md |
| 2026-09-01T00:00:00Z | /execute (decisions) | Locked in: solo team, Featherless AI for LLM (confirmed OpenAI-compatible API, tool calling support, Qwen/Llama/Mistral/DeepSeek model families), and hosting split — GitHub Actions cron running Alpaca CLI for the trading engine, Netlify for the public demo dashboard. Also decided CLI over MCP server after confirming Alpaca provides no hosted remote MCP server (MCP is built for interactive assistant sessions, not unattended cron). Surfaced two official Alpaca reference repos (options-wheel, gamma-scalping) as scaffold references. | architecture-decisions.md, README.md |
| 2026-09-01T00:00:00Z | /create-plan | Realized only ~3.5 days remain before the Sep 4 8:30 PM IST deadline (today is 2026-09-01, competition started Aug 28) — switched strategy-selection approach from live paper-trading A/B testing to backtest-and-select. Created phased build plan: Phase 0 accounts/access, Phase 1 backtest 4 candidate strategies (VRP iron condor, GEX positioning, event-driven, hybrid) against real historical Alpaca options data, Phase 2 build the live agent on the winning strategy, Phase 3 Netlify dashboard, Phase 4 submission packaging. Confirmed Alpaca's historical option bars endpoint exists (`/v1beta1/options/bars`) but depth/tier requirements need empirical testing once API keys are available. | ../plans/alpaca-trading-agent-build-plan.md |
| 2026-09-01T00:00:00Z | /execute (Phase 0/1) | Verified Alpaca paper API key (account PA37YZBHY6Q7, options level 3, $100k balance) and Featherless AI key both work live. Picked tiered LLM models: `meta-llama/Meta-Llama-3.1-8B-Instruct` (fast/cheap) and `deepseek-ai/DeepSeek-V3.2` (heavy reasoning). Ran Phase 1 data-access spike: Alpaca's historical option bars endpoint returns real daily OHLCV for expired SPY contracts (full contract lifetime confirmed), and SPY equity bars go back to Jan 2026 — real-data backtesting (Plan A) confirmed viable, no synthetic fallback needed. Dropped the event-driven earnings-IV-crush candidate from the backtest set: SPY (an ETF) has no earnings events, and early September has essentially no single-name earnings catalysts either (between Q2 August reporters and Q3 October reporters) — narrowed to 3 candidate strategies (VRP iron condor, GEX positioning, hybrid). | architecture-decisions.md, ../plans/alpaca-trading-agent-build-plan.md |
| 2026-09-01T00:00:00Z | /execute (Phase 1) | Discovered Alpaca's data API has no open-interest field anywhere (bars or snapshots) — true GEX is uncomputable; substituted put-call IV skew regime (real data, no new dependency). Built `backtest/` (data.py, black_scholes.py, metrics.py, strategies/{vrp_iron_condor,skew_regime,hybrid,common}.py, run_backtest.py). First run exposed a real bug: fixed 5%-OTM strike offset badly overshoots on 3-7 DTE options (near-zero premiums) — fixed by targeting delta (~0.175) directly via `common.delta_offset_pct()`. Backtested all 3 strategies against real historical SPY option prices over two overlapping lookback windows (30wk, 34wk); Hybrid VRP+Skew won on every risk-adjusted metric both times and was selected as the live strategy. Full numbers in backtest-results.md. | backtest-results.md, ../plans/alpaca-trading-agent-build-plan.md, architecture-decisions.md |
| 2026-09-02T00:00:00Z | /execute (Phase 2) | Confirmed strategy finalization with Adwik. Published a plain-English strategy explainer artifact (glossary + iron-condor payoff diagram). Built `agent/` (config, analyst, governor, executor, risk_gates, audit_log, run_agent) implementing the Analyst→Governor→Executor architecture. Downloaded Alpaca CLI as a Windows binary and verified real MLEG order submission end-to-end against the dev account. Found SPY has daily expiries and that "next Friday" (Sep 4) is NFP day — our delta-targeted strikes can't price that jump risk, so switched the live target to Sep 3. Two live LLM-analyst tests on the same day: one correctly flagged the NFP risk, one fabricated a Labor Day date fact — confirmed the design decision to keep the analyst advisory-only (logged, never gating). Cleaned up all test orders on the dev account after each run. | architecture-decisions.md, ../plans/alpaca-trading-agent-build-plan.md |
| 2026-09-02T00:00:00Z | /execute (backtest) | Re-ran the backtest over a full 1-year lookback (52 weeks) on user request, confirming real option data exists back to Sep 2025. Hybrid VRP+Skew still won every risk-adjusted metric (90.9% win rate, 4.89 profit factor, 1.99% max drawdown) though Skew Regime alone banked more raw dollars via more frequent trading. Decision unchanged — Hybrid's selectivity fits a one-shot live trade better than a higher-frequency strategy would. | backtest-results.md |
| 2026-09-02T00:00:00Z | /execute (bug fix) | User's "are these good results?" sanity-check question caught a real bug: metrics.py annualized Sharpe/Sortino with a hardcoded sqrt(252), assuming daily trading, when these strategies only trade 11-26 times/year — inflated the reported Sharpe 3-5x (Hybrid's 11.82 was never real). Fixed to annualize by actual trade frequency; corrected Hybrid Sharpe is 2.53 (still highest, ranking unchanged). Also benchmarked against buy-and-hold SPY over the same year (+18.9%, more than double every strategy) — expected since these are market-neutral premium-selling strategies, not directional bets. | backtest-results.md, backtest/metrics.py, backtest/run_backtest.py |
| 2026-09-02T00:00:00Z | /research + /execute | User raised a valid concern: Hybrid only fires ~11x/year, real risk of zero trades in the ~2-day remaining window. Delegated research on industry-standard intraday/0DTE SPY strategies (Antigravity) — 4 candidates identified (0DTE Iron Condor, ORB Breakout, VWAP Mean Reversion, VWAP Trend Pullback), all reusing existing Black-Scholes/executor/risk-gate code. Built `backtest/intraday/` and backtested all 4 over 6 months of real 5-min option data (126 trading days). Caught a real sign-bug in Strategy 3 (credit-spread pricer reused a debit-spread convention, falsely registering every trade as an instant win) before trusting results. Final finding: 3 of 4 intraday strategies lose money over 6 months; the best (0DTE Iron Condor, Sharpe 0.30) is far below Hybrid's Sharpe 2.53. Decision reaffirmed: stay with weekly Hybrid — also confirmed its entry gates are live-favorable for real, right now (not just a hopeful extrapolation from the base rate). | intraday-strategy-research.md, intraday-backtest-results.md, README.md |
| 2026-09-02T00:00:00Z | /execute | User asked to update the VRP+Skew logic itself for intraday use rather than abandon it. Found and fixed a real regime-mismatch bug (0DTE IV vs daily-close RV20 aren't comparable timescales - IV came in consistently below RV, producing zero trades in 10/10 test days). Fixed with a same-timescale (5-min, 15-day rolling) realized-vol estimate and a relative IV/RV ratio threshold instead of an absolute point-spread. 6-month result after the fix: 18 trades/yr equiv, 27.8% win rate, Sharpe -1.224 - the worst-performing strategy tested in the entire exploration. Conclusion: the weekly timescale isn't an implementation detail of Hybrid's edge, it's load-bearing - "IV exceeds RV" means something different (and less favorable) at 0DTE than at 4-7 DTE. Closed this line of investigation; reaffirmed weekly Hybrid VRP+Skew as the live strategy. | intraday-backtest-results.md |
| 2026-09-02T00:00:00Z | /research + /execute | User pushed further: wanted a genuinely return-competitive strategy (beats SPY's raw return, not just risk-adjusted), given Hybrid's +8.1%/yr vs SPY's +18.9%/yr gap. Delegated research (Antigravity) — 4 candidates: PMCC/call diagonal (top pick, bull-case 22-31% CAGR), bull risk reversal, 90/10 barbell, momentum verticals, all proposing 30-90 DTE entries. Building the real-data backtest surfaced two hard account-level data constraints: (1) Alpaca 403s ("OPRA agreement is not signed") for any option contract not yet expired, even for past days; (2) this account's SPY option data has essentially zero trade prints earlier than ~14 days before a contract's own expiry, regardless of moneyness (confirmed directly - deep ITM was worse than ATM/OTM, not better). Both forced all 4 candidates to be redesigned to ~12 DTE entries rather than the researched 30-90 DTE. Built `backtest/candidates/` (common.py, pmcc_diagonal.py, risk_reversal.py, barbell_call_spread.py, momentum_vertical.py, run_candidates_backtest.py). Caught and fixed 2 real bugs along the way: a PMCC accounting bug that silently dropped a position's value to zero on a no-print close date, and a None-leg crash when a mid-run roll failed. 26-week real-data results: PMCC (the #1 research pick) was catastrophic once compressed (-63.2% return, 86.2% max drawdown - a normal ~2% SPY pullback wiped out its "deep ITM" cushion at only 12 DTE); barbell and momentum roughly broke even or lost; risk reversal was the only respectable candidate (Sharpe 1.59, Sortino 3.22, max DD 3.23%) but still trails both SPY buy-and-hold (+11.17% over the same window) and Hybrid's own real 52-week numbers (Sharpe 2.53, max DD 1.99%). 4th consecutive same-day cycle to fail to beat Hybrid VRP+Skew — reaffirmed again as the live Phase 2 strategy. | candidate-strategy-backtest-results.md |
| 2026-09-02T00:00:00Z | /explore + /create-plan + /execute | User referenced karpathy/autoresearch (an autonomous LLM-agent loop that edits a training file, runs a bounded experiment, checks a metric, keeps/discards, repeats) and asked to adapt the pattern into a genuinely reusable tool, not hackathon-only scaffolding. Explored and scoped via 3 rounds of clarifying questions: target = 0DTE Iron Condor (highest trade volume among non-broken candidates, 84 trades/yr, needed for a real train/test split), scope = bounded parameter tuning only (not strategy rewriting), objective = maximize Sharpe subject to a 6% max-drawdown cap (matching agent/config.py's kill switch), mechanism = literal agent-in-the-loop (Claude reasons each iteration, not a deterministic script), overfitting guard = train/test split. Plan written to `.claude/plans/0dte-condor-parameter-tuning-plan.md`. Built `backtest/tuning/` (split, search_space, evaluator, ledger, CLI wrappers) and `.claude/skills/tune-0dte-condor/`; refactored `strategy_1_iron_condor.run()` to accept parameter overrides without touching the file per attempt. Ran the full 19-iteration search directly (not via literal `/loop` scheduling - disclosed deviation, ScheduleWakeup's real-time pacing added no value for a bounded same-session task). Found a real improvement on the train split (Sharpe -0.95 → 3.41: further-OTM strikes + wider wings + a tighter calm-market entry filter), and the search's own 15-trade eligibility floor correctly caught and rejected one spuriously perfect result (Sharpe 6.76 off only 14 trades) before it could contaminate the outcome. The held-out test-split validation, however, came back inconclusive: the untouched baseline scored even better than the tuned parameters there (Sharpe 18.3 vs 12.2, both implausibly high) - a genuine regime shift between the two 2026 windows (calm Jul-Sep vs choppier Mar-Jun) made the test split uninformative rather than confirmatory. Reported honestly rather than leading with the flashy test number; final parameters proposed as a research finding for further validation, not a proven, ready-to-apply result - awaiting explicit confirmation before touching the strategy file. | 0dte-condor-tuning-results.md, ../plans/0dte-condor-parameter-tuning-plan.md |
| 2026-09-03T00:00:00Z | /execute (Phase 2 Step 7) | Deadline now ~1 day out (Sep 4, 8:30 PM IST) - redirected focus from research work to the still-unbuilt submission critical path (fresh account, GH Actions cron, dashboard, write-up), none of which was started as of 2026-09-02. User created the fresh, dedicated Alpaca paper account for judging. First credential-fill attempt landed in the wrong place (`.env.local` showed genuinely empty values on direct check - not a parsing bug, confirmed via awk/hexdump); second attempt succeeded. Verified live via `/v2/account` rather than trusting the file alone: account `PA3WNF5ZV8W3`, confirmed distinct from the dev account `PA37YZBHY6Q7` used throughout this project so far, cash and equity both exactly $100,000, options approved level 3, status ACTIVE, created 2026-09-03 - satisfies the challenge's "fresh, dedicated, $100k, not reused" requirement. Also discovered no git repository exists yet for this project (blocking both GH Actions and the public-repo submission requirement) - ran `git init` locally (no commit yet, pending explicit go-ahead) and confirmed `.gitignore` already correctly excludes `.env.local` and other secrets/caches. | ../plans/alpaca-trading-agent-build-plan.md |
| 2026-09-03T00:00:00Z | /execute (Phase 2 Step 8) | Built `.github/workflows/trading-agent.yml` (cron every 15 min during market hours, installs Alpaca CLI via `go install` since CI is Linux and the local binary is Windows-only, runs `agent.run_agent` against the submission account, commits the audit log after each run) and `requirements.txt`. Found and cleaned up 2 stale duplicate per-module requirements files (`agent/requirements.txt`, `backtest/requirements.txt` - the latter listed `pandas`, confirmed unused anywhere in the codebase) before consolidating on one root file. Created the public GitHub repo (`github.com/adwik1401/alpaca-ai-trading-agent`, satisfies the challenge's public-repo requirement) and pushed the initial commit (77 files) - caught a real gitignore gap first (`backtest/.cache/` only matched the top-level weekly cache dir, not `backtest/intraday/.cache/`, which was about to get committed) and fixed it to `**/.cache/` before staging. Smoke-tested the full agent cycle locally against the real submission account (circuit breakers, live signal evaluation, Featherless analyst debate, and the Alpaca CLI itself all verified working end-to-end - no trade fired that cycle, a legitimate outcome). Blocked on the last step: Claude Code's safety classifier refused to pipe `.env.local` secret values into `gh secret set` even non-displaying, so the 4 GitHub Actions repository secrets need to be set by Adwik directly (command or web UI both provided) before the cron can actually run - time-sensitive since 2026-09-03 (today) is the actual decided live-trading target date (Sep 4 is NFP day, avoided per architecture-decisions.md), not just a day before the submission deadline. | ../plans/alpaca-trading-agent-build-plan.md |
